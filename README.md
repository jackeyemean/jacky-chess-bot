Currently incomplete, to view data that the model is trained on navigate to data/positions_labelled.csv.

Below is the Pygame GUI used to populate the data with the user's candidate moves.

![alt text](assets/image.png)